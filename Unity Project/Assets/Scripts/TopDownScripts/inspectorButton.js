#pragma strict
/*
@CustomEditor (topDownCameraScript)
public class RunMethod extends Editor
{
    function OnInspectorGUI()
    {
        DrawDefaultInspector();
        
        var myScript : topDownCameraScript = target;
        if(GUILayout.Button("Change Camera Mode"))
        {
            myScript.ChangeCameraMode();
        }
    }
}
*/